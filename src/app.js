import React from 'react';
import reactDOM from 'react-dom';
import Provider from 'react-redux';
//import app
//import store and connect with provider
const app = document.body.querySelector('.app');

reactDOM.render(
  <section>Hello</section>,
  app
);
